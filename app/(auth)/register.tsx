import { SafeAreaView } from "react-native-safe-area-context";

import {Text} from 'react-native';


export default function Register(){

    return <SafeAreaView>
        <Text className="text-center font-semibold">Page Register</Text>
    </SafeAreaView>
}